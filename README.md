# Spam-Filter-Using-Regular-Expressions
A simple web application that checks if a message contains common spam patterns such as "free money", "please help", "stock alerts", or large dollar amounts. Built using HTML, CSS, and JavaScript with regex-based detection for realistic spam variations.
